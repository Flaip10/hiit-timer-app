import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    btn: {
        width: 36,
        height: 36,
        borderRadius: 8,
        alignItems: 'center',
        justifyContent: 'center',
    },
    pressed: {
        opacity: 0.75,
    },
});
